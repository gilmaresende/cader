import { IkMaskDirective } from './ik-mask.directive';

describe('IkMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new IkMaskDirective();
    expect(directive).toBeTruthy();
  });
});
