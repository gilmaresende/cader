import { IkMoneyDirective } from './ik-money.directive';

describe('IkMoneyDirective', () => {
  it('should create an instance', () => {
    const directive = new IkMoneyDirective();
    expect(directive).toBeTruthy();
  });
});
